# Local development database only; never put credentials in this file.
DATABASE_URL="file:./db/custom.db"

# Google Apps Script Web App URL for the read-only FACILITIES API.
# Keep this server-side; do not prefix it with NEXT_PUBLIC_.
FACILITY_API_URL="https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec"
